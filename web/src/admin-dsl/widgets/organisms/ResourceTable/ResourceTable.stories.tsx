/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-19T00:32:13+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/06-resource-widgets.yaml
 * Source YAML last commit: e555038 2026-05-18T17:11:02-04:00 HAIR-041 Step 48: Fill widget IR YAML intent
 * Target file previous commit: baf8866 2026-05-18T17:21:36-04:00 HAIR-041 Step 50: Regenerate widget scaffolds from schema v2
 * Widget ID: admin.resource.resource-table
 *
 * This file is generated from schema-v2 Widget Definition IR. Keep raw Admin DSL
 * JSON decoding in adapters; generated widgets should receive typed props only.
 */
import type { Meta, StoryObj } from "@storybook/react";
import { ResourceTable } from "./ResourceTable";
import type { ResourceTableProps } from "./ResourceTable.types";

const defaultArgs = {
  tableId: "requests",
  columns: [
    { id: "customer", label: "Customer", kind: "text", primary: true },
    { id: "status", label: "Status", kind: "badge" },
  ],
  rows: [
    { id: "req_1", customer: "Maya Chen", status: "new" },
    { id: "req_2", customer: "Jules Park", status: "needsInfo" },
  ],
  page: 1,
  total: 2,
  selectable: true,
  bulkActions: [{ type: "mutation", target: "requests.assign", label: "Assign" }],
} as unknown as ResourceTableProps;

const meta = {
  title: "Admin DSL Widgets/Organisms/ResourceTable",
  component: ResourceTable,
  args: defaultArgs,
  parameters: {
    docs: {
      description: {
        component: "Render semantic resource rows as a desktop table and mobile card list. This is the most important information-dense Admin DSL widget.",
      },
    },
  },
} satisfies Meta<typeof ResourceTable>;

export default meta;
type Story = StoryObj<typeof meta>;

export const DefaultRows: Story = {
  name: "DefaultRows",
  parameters: { docs: { description: { story: "Tests ResourceTable with normal populated rows and standard dense table spacing." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "defaultRows"
}, asserts: [
  "ResourceTable renders the DefaultRows scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const SelectableWithBulkToolbar: Story = {
  name: "SelectableWithBulkToolbar",
  parameters: { docs: { description: { story: "Tests selection-enabled ResourceTable with visible/selected bulk action toolbar." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "selectableWithBulkToolbar"
}, asserts: [
  "ResourceTable renders the SelectableWithBulkToolbar scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const RowActions: Story = {
  name: "RowActions",
  parameters: { docs: { description: { story: "Tests row-scoped actions in ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "rowActions"
}, asserts: [
  "ResourceTable renders the RowActions scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const OverflowActions: Story = {
  name: "OverflowActions",
  parameters: { docs: { description: { story: "Tests compact overflow action presentation in ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "overflowActions"
}, asserts: [
  "ResourceTable renders the OverflowActions scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const EmptyState: Story = {
  name: "EmptyState",
  parameters: { docs: { description: { story: "Tests empty-state substitution or composition in ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "emptyState"
}, asserts: [
  "ResourceTable renders the EmptyState scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const Pagination: Story = {
  name: "Pagination",
  parameters: { docs: { description: { story: "Tests pagination footer state and actions in ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "pagination"
}, asserts: [
  "ResourceTable renders the Pagination scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const StatusBadgeCells: Story = {
  name: "StatusBadgeCells",
  parameters: { docs: { description: { story: "Tests mapped status cell rendering in ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "statusBadgeCells"
}, asserts: [
  "ResourceTable renders the StatusBadgeCells scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const DragHandleColumn: Story = {
  name: "DragHandleColumn",
  parameters: { docs: { description: { story: "Tests drag-handle affordance cells in ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "dragHandleColumn"
}, asserts: [
  "ResourceTable renders the DragHandleColumn scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const MobileCards: Story = {
  name: "MobileCards",
  parameters: { docs: { description: { story: "Tests mobile card adaptation for ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "mobileCards"
}, asserts: [
  "ResourceTable renders the MobileCards scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const LongTextAndDenseRows: Story = {
  name: "LongTextAndDenseRows",
  parameters: { docs: { description: { story: "Tests long text, wrapping, and dense row spacing in ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "longTextAndDenseRows"
}, asserts: [
  "ResourceTable renders the LongTextAndDenseRows scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const ActionDispatchContexts: Story = {
  name: "ActionDispatchContexts",
  parameters: { docs: { description: { story: "Tests row and bulk action callback contexts emitted by ResourceTable." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTable {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "actionDispatchContexts"
}, asserts: [
  "ResourceTable renders the ActionDispatchContexts scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
