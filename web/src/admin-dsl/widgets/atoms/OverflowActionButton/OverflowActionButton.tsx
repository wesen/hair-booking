/**
 * GENERATED SCAFFOLD — DO NOT TREAT AS FINISHED IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-18T20:40:03+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/04-action-widgets.yaml
 * Source YAML last commit: ec60525 2026-05-18T16:21:43-04:00 HAIR-041 Step 43: Add widget IR YAML artifacts
 * Target file previous commit: none (untracked or no history)
 *
 * This header is the first changelog entry for this generated scaffold. When a
 * human replaces generated placeholders, keep the generated provenance above and
 * add implementation notes below it rather than deleting the history.
 */
/**
 * Widget IR: OverflowActionButton
 * Category: action_widgets
 * Classification: {"level": "atom"}
 * Current source inventory: renderTableCell branch for kind === "overflowActions"
 *
 * Purpose:
 * Render the ellipsis affordance for row overflow menus. First implementation can dispatch the first action, matching current behavior. Later implementation can open a real menu.
 *
 * Human notes from YAML:
 * {
 *   "intent": "Represents row overflow affordance as a named atom so the current ellipsis behavior can later grow into a real menu without changing ResourceTable cell contracts.",
 *   "adapter_boundary": "The table cell passes row context and available actions. The widget should not fetch row actions from raw row JSON.",
 *   "slot_intent": {
 *     "overflowActions": "Overflow actions are row-scoped actions with compact presentation. Initial scaffold can invoke the first action, but final implementation should open a menu."
 *   },
 *   "storybook_intent": "Stories should distinguish single-action shortcut behavior from future multi-action menu behavior."
 * }
 *
 * Action slots / callback intent:
 * [
 *   {
 *     "name": "overflowActions",
 *     "notes": "Uses the provided context and selected action.",
 *     "callback_prop": "onAction",
 *     "intent": "Overflow actions are row-scoped actions with compact presentation. Initial scaffold can invoke the first action, but final implementation should open a menu.",
 *     "implementation_note": "The widget emits typed context through the callback; the Admin DSL adapter lowers it to dispatchAdminAction or backend action transport."
 *   }
 * ]
 *
 * XXX items from YAML:
 * - XXX: Implement menu behavior before supporting multiple overflow actions.
 */
import type * as React from "react";
import type { ReactNode } from "react";
import type { OverflowActionButtonProps } from "./OverflowActionButton.types";

const widgetClassification = "{\"level\": \"atom\"}";
const widgetPurpose = "Render the ellipsis affordance for row overflow menus. First implementation can dispatch the first action, matching current behavior. Later implementation can open a real menu.";
const widgetHumanNotes = "{\n  \"intent\": \"Represents row overflow affordance as a named atom so the current ellipsis behavior can later grow into a real menu without changing ResourceTable cell contracts.\",\n  \"adapter_boundary\": \"The table cell passes row context and available actions. The widget should not fetch row actions from raw row JSON.\",\n  \"slot_intent\": {\n    \"overflowActions\": \"Overflow actions are row-scoped actions with compact presentation. Initial scaffold can invoke the first action, but final implementation should open a menu.\"\n  },\n  \"storybook_intent\": \"Stories should distinguish single-action shortcut behavior from future multi-action menu behavior.\"\n}";
const widgetActionSlots = "[\n  {\n    \"name\": \"overflowActions\",\n    \"notes\": \"Uses the provided context and selected action.\",\n    \"callback_prop\": \"onAction\",\n    \"intent\": \"Overflow actions are row-scoped actions with compact presentation. Initial scaffold can invoke the first action, but final implementation should open a menu.\",\n    \"implementation_note\": \"The widget emits typed context through the callback; the Admin DSL adapter lowers it to dispatchAdminAction or backend action transport.\"\n  }\n]";
const widgetXxx = "XXX: Implement menu behavior before supporting multiple overflow actions.";

/**
 * Scaffold for `OverflowActionButton`.
 *
 * Generated from `/home/manuel/workspaces/2026-04-21/hair-v2/hair-booking/ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/04-action-widgets.yaml`.
 * Current source inventory: renderTableCell branch for kind === "overflowActions".
 *
 * XXX: Replace this placeholder body with the real visual implementation. Keep
 * raw Admin DSL node decoding in renderer adapters; this component should
 * receive typed widget props only.
 */
export function OverflowActionButton(props: OverflowActionButtonProps) {
  const scaffoldProps = props as OverflowActionButtonProps & {
    id?: string;
    className?: string;
    style?: React.CSSProperties;
    children?: ReactNode;
    title?: string;
    label?: string;
    name?: string;
    value?: unknown;
  };
  const heading = scaffoldProps.title || scaffoldProps.label || scaffoldProps.name || "OverflowActionButton";

  return (
    <section
      id={scaffoldProps.id}
      className={scaffoldProps.className}
      data-admin-dsl-widget="OverflowActionButton"
      data-admin-dsl-widget-level="atom"
      style={scaffoldProps.style}
    >
      <div style={{ border: "1px dashed #c8b89b", borderRadius: 12, padding: 12, background: "#fffaf0" }}>
        <strong>{heading}</strong>
        <div style={{ marginTop: 4, fontSize: 12, color: "#6f6254" }}>
          OverflowActionButton scaffold — XXX replace with final implementation.
        </div>
        <dl style={{ margin: "10px 0 0", display: "grid", gap: 6, fontSize: 12, color: "#6f6254" }}>
          <div><dt style={{ fontWeight: 700 }}>Classification</dt><dd style={{ margin: 0, whiteSpace: "pre-wrap" }}>{widgetClassification}</dd></div>
          <div><dt style={{ fontWeight: 700 }}>Purpose</dt><dd style={{ margin: 0, whiteSpace: "pre-wrap" }}>{widgetPurpose}</dd></div>
          <div><dt style={{ fontWeight: 700 }}>Human notes</dt><dd style={{ margin: 0, whiteSpace: "pre-wrap" }}>{widgetHumanNotes}</dd></div>
          <div><dt style={{ fontWeight: 700 }}>Action slots</dt><dd style={{ margin: 0, whiteSpace: "pre-wrap" }}>{widgetActionSlots}</dd></div>
          <div><dt style={{ fontWeight: 700 }}>Implementation warnings</dt><dd style={{ margin: 0, whiteSpace: "pre-wrap" }}>{widgetXxx}</dd></div>
        </dl>
      </div>
      {scaffoldProps.children ? <div style={{ marginTop: 12 }}>{scaffoldProps.children}</div> : null}
    </section>
  );
}
