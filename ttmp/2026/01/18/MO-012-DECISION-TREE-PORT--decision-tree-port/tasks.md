# Tasks

## TODO

- [ ] Add tasks here

- [ ] Define SQLite schema + migration for decision_trees and bookings
- [ ] Implement Go DSL parser + validator (YAML) with unit tests
- [ ] Implement decision tree engine (pricing/duration/rules) + tests
- [ ] Build Go REST API for decision trees + bookings (no auth)
- [ ] Scaffold Vite React UI with RTK Query + Bootstrap
- [ ] Implement Intake + Summary flows in UI
- [ ] Implement Admin YAML editor (no AI assistant)
- [ ] Add go:embed + go generate asset pipeline with /static/ prefix
- [ ] Wire Makefile targets for dev/build and validate SPA fallback
