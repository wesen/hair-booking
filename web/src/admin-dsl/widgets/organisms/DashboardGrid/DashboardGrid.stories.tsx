/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-18T21:21:08+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/05-layout-widgets.yaml
 * Source YAML last commit: e555038 2026-05-18T17:11:02-04:00 HAIR-041 Step 48: Fill widget IR YAML intent
 * Target file previous commit: dfc6ac6 2026-05-18T16:41:42-04:00 HAIR-041 Step 44: Scaffold Admin DSL widgets from IR
 * Widget ID: admin.layout.dashboard-grid
 *
 * This file is generated from schema-v2 Widget Definition IR. Keep raw Admin DSL
 * JSON decoding in adapters; generated widgets should receive typed props only.
 */
import type { Meta, StoryObj } from "@storybook/react";
import { DashboardGrid } from "./DashboardGrid";
import type { DashboardGridProps } from "./DashboardGrid.types";

const defaultArgs = {
  columns: { desktop: 12, tablet: 8, mobile: 1 },
  gap: "compact",
  children: <div>Grid item</div>,
} as unknown as DashboardGridProps;

const meta = {
  title: "Admin DSL Widgets/Organisms/DashboardGrid",
  component: DashboardGrid,
  args: defaultArgs,
  parameters: {
    docs: {
      description: {
        component: "Owns responsive dashboard grid layout and item span/order behavior instead of scattering grid styles across Admin DSL render branches.",
      },
    },
  },
} satisfies Meta<typeof DashboardGrid>;

export default meta;
type Story = StoryObj<typeof meta>;

export const TwelveColumnDesktop: Story = {
  name: "TwelveColumnDesktop",
  parameters: { docs: { description: { story: "Tests the TwelveColumnDesktop scenario for DashboardGrid with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <DashboardGrid {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "twelveColumnDesktop"
}, asserts: [
  "DashboardGrid renders the TwelveColumnDesktop scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const CompactGap: Story = {
  name: "CompactGap",
  parameters: { docs: { description: { story: "Tests the CompactGap scenario for DashboardGrid with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <DashboardGrid {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "compactGap"
}, asserts: [
  "DashboardGrid renders the CompactGap scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const MixedSpanCards: Story = {
  name: "MixedSpanCards",
  parameters: { docs: { description: { story: "Tests the MixedSpanCards scenario for DashboardGrid with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <DashboardGrid {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "mixedSpanCards"
}, asserts: [
  "DashboardGrid renders the MixedSpanCards scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const MobileSingleColumn: Story = {
  name: "MobileSingleColumn",
  parameters: { docs: { description: { story: "Tests the MobileSingleColumn scenario for DashboardGrid with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <DashboardGrid {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "mobileSingleColumn"
}, asserts: [
  "DashboardGrid renders the MobileSingleColumn scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const Ordering: Story = {
  name: "Ordering",
  parameters: { docs: { description: { story: "Tests the Ordering scenario for DashboardGrid with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <DashboardGrid {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "ordering"
}, asserts: [
  "DashboardGrid renders the Ordering scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
