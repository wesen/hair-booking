/**
 * GENERATED SCAFFOLD — DO NOT TREAT AS FINISHED IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-18T20:40:03+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/04-action-widgets.yaml
 * Source YAML last commit: ec60525 2026-05-18T16:21:43-04:00 HAIR-041 Step 43: Add widget IR YAML artifacts
 * Target file previous commit: none (untracked or no history)
 *
 * This header is the first changelog entry for this generated scaffold. When a
 * human replaces generated placeholders, keep the generated provenance above and
 * add implementation notes below it rather than deleting the history.
 */
/**
 * Widget IR: ActionButton
 * Category: action_widgets
 * Classification: {"level": "atom"}
 * Current source inventory: renderActions button branch in web/src/admin-dsl/render.tsx plus row/bulk/search special buttons
 *
 * Purpose:
 * Render one action with consistent disabled, loading, primary, danger, subtle, and placement-aware styling.
 *
 * Human notes from YAML:
 * {
 *   "intent": "Unifies visual treatment for action metadata so primary, danger, subtle, loading, disabled, and touch-target rules do not get reimplemented in every table/panel/form branch.",
 *   "adapter_boundary": "The widget receives an ActionViewModel and context. It should not know AdminNode or dispatchAdminAction directly.",
 *   "slot_intent": {
 *     "self": "The caller chooses the semantic slot and passes an onAction callback; ActionButton only renders and invokes."
 *   },
 *   "storybook_intent": "Stories should pin visual variants and prevent regressions in button shape, subtle row links, danger styling, and loading/disabled states."
 * }
 *
 * Action slots / callback intent:
 * [
 *   {
 *     "name": "self",
 *     "notes": "The widget executes the action it receives through onAction.",
 *     "callback_prop": "onAction",
 *     "intent": "The caller chooses the semantic slot and passes an onAction callback; ActionButton only renders and invokes.",
 *     "implementation_note": "The widget emits typed context through the callback; the Admin DSL adapter lowers it to dispatchAdminAction or backend action transport."
 *   }
 * ]
 *
 * XXX items from YAML:
 * - XXX: Replace placeholder with actual button element styles from design tokens.
 * - XXX: Map variant/slot semantics without reintroducing placement-specific conditionals everywhere.
 */
// XXX: Replace generated defaultArgs with purposeful fixtures for every planned story before treating this as visual coverage.
import type { Meta, StoryObj } from "@storybook/react";
import { ActionButton } from "./ActionButton";
import type { ActionButtonProps } from "./ActionButton.types";

const defaultArgs = {
  action: { type: "mutation", target: "scaffold.action", label: "Run action" },
} as unknown as ActionButtonProps;

const meta = {
  title: "Admin DSL Widgets/Atoms/ActionButton",
  component: ActionButton,
  args: defaultArgs,
} satisfies Meta<typeof ActionButton>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Primary: Story = {};
export const Secondary: Story = {};
export const Danger: Story = {};
export const SubtleLink: Story = {};
export const Loading: Story = {};
export const Disabled: Story = {};
export const TouchTargetMobile: Story = {};
