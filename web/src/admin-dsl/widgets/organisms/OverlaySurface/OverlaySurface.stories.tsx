/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-18T21:21:08+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/11-surface-widgets.yaml
 * Source YAML last commit: e555038 2026-05-18T17:11:02-04:00 HAIR-041 Step 48: Fill widget IR YAML intent
 * Target file previous commit: none (untracked or no history)
 * Widget ID: admin.surface.overlay-surface
 *
 * This file is generated from schema-v2 Widget Definition IR. Keep raw Admin DSL
 * JSON decoding in adapters; generated widgets should receive typed props only.
 */
import type { Meta, StoryObj } from "@storybook/react";
import { OverlaySurface } from "./OverlaySurface";
import type { OverlaySurfaceProps } from "./OverlaySurface.types";

const defaultArgs = {
  surfaceId: "drawer_1",
  kind: "drawer",
  title: "Review request",
  children: <div>Surface content</div>,
} as unknown as OverlaySurfaceProps;

const meta = {
  title: "Admin DSL Widgets/Organisms/OverlaySurface",
  component: OverlaySurface,
  args: defaultArgs,
  parameters: {
    docs: {
      description: {
        component: "OverlaySurface widget.",
      },
    },
  },
} satisfies Meta<typeof OverlaySurface>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Modal: Story = {
  name: "Modal",
  parameters: { docs: { description: { story: "Tests the Modal scenario for OverlaySurface with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "modal"
}, asserts: [
  "OverlaySurface renders the Modal scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const Drawer: Story = {
  name: "Drawer",
  parameters: { docs: { description: { story: "Tests the Drawer scenario for OverlaySurface with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "drawer"
}, asserts: [
  "OverlaySurface renders the Drawer scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const Sheet: Story = {
  name: "Sheet",
  parameters: { docs: { description: { story: "Tests the Sheet scenario for OverlaySurface with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "sheet"
}, asserts: [
  "OverlaySurface renders the Sheet scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const DetailPanel: Story = {
  name: "DetailPanel",
  parameters: { docs: { description: { story: "Tests the DetailPanel scenario for OverlaySurface with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "detailPanel"
}, asserts: [
  "OverlaySurface renders the DetailPanel scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const InlinePanel: Story = {
  name: "InlinePanel",
  parameters: { docs: { description: { story: "Tests the InlinePanel scenario for OverlaySurface with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "inlinePanel"
}, asserts: [
  "OverlaySurface renders the InlinePanel scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const ClosedDashedPreview: Story = {
  name: "ClosedDashedPreview",
  parameters: { docs: { description: { story: "Tests closed/preview styling for OverlaySurface." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "closedDashedPreview"
}, asserts: [
  "OverlaySurface renders the ClosedDashedPreview scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const WithFooterActions: Story = {
  name: "WithFooterActions",
  parameters: { docs: { description: { story: "Tests footer action slot rendering for OverlaySurface." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "withFooterActions"
}, asserts: [
  "OverlaySurface renders the WithFooterActions scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const MobileSurface: Story = {
  name: "MobileSurface",
  parameters: { docs: { description: { story: "Tests the MobileSurface scenario for OverlaySurface with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <OverlaySurface {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "mobileSurface"
}, asserts: [
  "OverlaySurface renders the MobileSurface scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
