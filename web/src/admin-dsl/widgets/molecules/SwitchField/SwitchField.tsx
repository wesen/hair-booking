/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/10a-form-field-widgets.yaml
 * Widget ID: admin.form-field.switch-field
 *
 * Manual edits after generation:
 * - 2026-05-19 / HAIR-041 Step 126: Replaced scaffold diagnostics with typed checkbox switch using FieldShell.
 */
import { FieldShell, fieldDescriptionIds } from "../../atoms/FieldShell";
import { adminTextStyle, adminTokens, dataAttrsFromRecord, widgetDataAttributes } from "../../shared";
import { switchFieldWidgetMetadata } from "./SwitchField.metadata";
import type { SwitchFieldProps } from "./SwitchField.types";

export function SwitchField({ id, className, style, dataAttributes, name, label, checked = false, helpText, error, disabled = false, readOnly = false, required = false, onValueChange }: SwitchFieldProps) {
  const controlId = id || `admin-field-${name}`;
  const effectiveDisabled = disabled || readOnly;
  const trackBackground = checked ? adminTokens.text.accent : adminTokens.borders.default;
  return (
    <FieldShell id={id ? `${id}-shell` : undefined} name={name} label={label} controlId={controlId} helpText={helpText} error={error} disabled={effectiveDisabled} readOnly={readOnly} required={required} style={style} dataAttributes={dataAttributes}>
      <label
        className={["adminDslSwitchField", className].filter(Boolean).join(" ") || undefined}
        style={{
          display: "inline-flex",
          alignItems: "center",
          gap: 10,
          width: "fit-content",
          cursor: effectiveDisabled ? "not-allowed" : "pointer",
          ...adminTextStyle("body"),
          color: effectiveDisabled ? adminTokens.text.muted : adminTokens.text.primary,
        }}
      >
        <input
          id={controlId}
          name={name}
          type="checkbox"
          defaultChecked={checked}
          disabled={effectiveDisabled}
          required={required}
          aria-invalid={error ? true : undefined}
          aria-describedby={fieldDescriptionIds({ controlId, helpText, error })}
          style={{ position: "absolute", opacity: 0, pointerEvents: "none" }}
          onChange={onValueChange ? (event) => onValueChange(event.currentTarget.checked, event) : undefined}
          {...widgetDataAttributes(switchFieldWidgetMetadata.widgetId, switchFieldWidgetMetadata.classification.level)}
          {...dataAttrsFromRecord({ field: name, checked: checked ? "true" : "false", invalid: error ? "true" : undefined })}
        />
        <span aria-hidden="true" style={{ width: 38, height: 22, borderRadius: adminTokens.radii.pill, background: trackBackground, border: `1px solid ${error ? adminTokens.borders.danger : adminTokens.borders.default}`, position: "relative", display: "inline-flex", alignItems: "center", padding: 2, boxSizing: "border-box" }}>
          <span style={{ width: 16, height: 16, borderRadius: adminTokens.radii.pill, background: adminTokens.surfaces.panel, transform: checked ? "translateX(16px)" : "translateX(0)", transition: "transform 120ms ease" }} />
        </span>
        <span>{checked ? "On" : "Off"}</span>
      </label>
    </FieldShell>
  );
}
