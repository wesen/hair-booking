/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-19T04:13:24+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/10a-form-field-widgets.yaml
 * Widget ID: admin.form-field.select-field
 *
 * Manual edits after generation:
 * - 2026-05-19 / HAIR-041 Step 125: Replaced scaffold diagnostics with typed select control using FieldShell.
 */
import { FieldShell, fieldDescriptionIds } from "../../atoms/FieldShell";
import { adminTextStyle, adminTokens, dataAttrsFromRecord, widgetDataAttributes } from "../../shared";
import { selectFieldWidgetMetadata } from "./SelectField.metadata";
import type { SelectFieldProps } from "./SelectField.types";

export function SelectField({ id, className, style, dataAttributes, name, label, value, options, placeholder, helpText, error, disabled = false, readOnly = false, required = false, onValueChange }: SelectFieldProps) {
  const controlId = id || `admin-field-${name}`;
  const effectiveDisabled = disabled || readOnly;
  const hasValue = value != null && value !== "";
  return (
    <FieldShell id={id ? `${id}-shell` : undefined} name={name} label={label} controlId={controlId} helpText={helpText} error={error} disabled={effectiveDisabled} readOnly={readOnly} required={required} style={style} dataAttributes={dataAttributes}>
      <select
        id={controlId}
        name={name}
        defaultValue={hasValue ? value : ""}
        disabled={effectiveDisabled}
        required={required}
        aria-invalid={error ? true : undefined}
        aria-describedby={fieldDescriptionIds({ controlId, helpText, error })}
        className={["adminDslSelectField", className].filter(Boolean).join(" ") || undefined}
        style={{
          ...adminTextStyle("body"),
          minHeight: 42,
          border: `1px solid ${error ? adminTokens.borders.danger : adminTokens.borders.default}`,
          borderRadius: adminTokens.radii.control,
          padding: "10px 12px",
          background: readOnly ? adminTokens.surfaces.muted : adminTokens.surfaces.panel,
          color: adminTokens.text.primary,
          outlineColor: adminTokens.text.accent,
        }}
        onChange={onValueChange ? (event) => onValueChange(event.currentTarget.value, event) : undefined}
        {...widgetDataAttributes(selectFieldWidgetMetadata.widgetId, selectFieldWidgetMetadata.classification.level)}
        {...dataAttrsFromRecord({ field: name, invalid: error ? "true" : undefined })}
      >
        {placeholder ? <option value="" disabled={required}>{placeholder}</option> : null}
        {options.map((option) => <option key={option.value} value={option.value} disabled={option.disabled}>{option.label}</option>)}
      </select>
    </FieldShell>
  );
}
