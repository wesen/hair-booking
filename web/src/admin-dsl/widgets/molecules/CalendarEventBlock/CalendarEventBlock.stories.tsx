/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-18T21:21:08+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/09-calendar-widgets.yaml
 * Source YAML last commit: e555038 2026-05-18T17:11:02-04:00 HAIR-041 Step 48: Fill widget IR YAML intent
 * Target file previous commit: none (untracked or no history)
 * Widget ID: admin.calendar.calendar-event-block
 *
 * This file is generated from schema-v2 Widget Definition IR. Keep raw Admin DSL
 * JSON decoding in adapters; generated widgets should receive typed props only.
 */
import type { Meta, StoryObj } from "@storybook/react";
import { CalendarEventBlock } from "./CalendarEventBlock";
import type { CalendarEventBlockProps } from "./CalendarEventBlock.types";

const defaultArgs = {
  id: "block_1",
  kind: "appointment",
  title: "Cut + color",
} as unknown as CalendarEventBlockProps;

const meta = {
  title: "Admin DSL Widgets/Molecules/CalendarEventBlock",
  component: CalendarEventBlock,
  args: defaultArgs,
  parameters: {
    docs: {
      description: {
        component: "CalendarEventBlock widget.",
      },
    },
  },
} satisfies Meta<typeof CalendarEventBlock>;

export default meta;
type Story = StoryObj<typeof meta>;

export const Appointment: Story = {
  name: "Appointment",
  parameters: { docs: { description: { story: "Tests the Appointment scenario for CalendarEventBlock with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <CalendarEventBlock {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "appointment"
}, asserts: [
  "CalendarEventBlock renders the Appointment scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const Availability: Story = {
  name: "Availability",
  parameters: { docs: { description: { story: "Tests the Availability scenario for CalendarEventBlock with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <CalendarEventBlock {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "availability"
}, asserts: [
  "CalendarEventBlock renders the Availability scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const TimeOff: Story = {
  name: "TimeOff",
  parameters: { docs: { description: { story: "Tests the TimeOff scenario for CalendarEventBlock with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <CalendarEventBlock {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "timeOff"
}, asserts: [
  "CalendarEventBlock renders the TimeOff scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const CompactBlock: Story = {
  name: "CompactBlock",
  parameters: { docs: { description: { story: "Tests the CompactBlock scenario for CalendarEventBlock with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <CalendarEventBlock {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "compactBlock"
}, asserts: [
  "CalendarEventBlock renders the CompactBlock scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const AgendaItem: Story = {
  name: "AgendaItem",
  parameters: { docs: { description: { story: "Tests the AgendaItem scenario for CalendarEventBlock with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <CalendarEventBlock {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "agendaItem"
}, asserts: [
  "CalendarEventBlock renders the AgendaItem scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
