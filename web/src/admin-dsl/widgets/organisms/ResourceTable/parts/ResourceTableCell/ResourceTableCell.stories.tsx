/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-19T00:32:13+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/06-resource-widgets.yaml
 * Source YAML last commit: e555038 2026-05-18T17:11:02-04:00 HAIR-041 Step 48: Fill widget IR YAML intent
 * Target file previous commit: baf8866 2026-05-18T17:21:36-04:00 HAIR-041 Step 50: Regenerate widget scaffolds from schema v2
 * Widget ID: admin.resource.resource-table-cell
 *
 * This file is generated from schema-v2 Widget Definition IR. Keep raw Admin DSL
 * JSON decoding in adapters; generated widgets should receive typed props only.
 */
import type { Meta, StoryObj } from "@storybook/react";
import { ResourceTableCell } from "./ResourceTableCell";
import type { ResourceTableCellProps } from "./ResourceTableCell.types";

const defaultArgs = {
  tableId: "requests",
  column: { id: "customer", label: "Customer", kind: "text", primary: true },
  row: { id: "req_1", customer: "Maya Chen" },
} as unknown as ResourceTableCellProps;

const meta = {
  title: "Admin DSL Widgets/Molecules/ResourceTableCell",
  component: ResourceTableCell,
  args: defaultArgs,
  parameters: {
    docs: {
      description: {
        component: "Localizes cell-kind rendering so ResourceTable is not a pile of conditional JSX. It can later become a registry of cell renderers.",
      },
    },
  },
} satisfies Meta<typeof ResourceTableCell>;

export default meta;
type Story = StoryObj<typeof meta>;

export const TextCell: Story = {
  name: "TextCell",
  parameters: { docs: { description: { story: "Tests the TextCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "textCell"
}, asserts: [
  "ResourceTableCell renders the TextCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const PrimaryTextCell: Story = {
  name: "PrimaryTextCell",
  parameters: { docs: { description: { story: "Tests the PrimaryTextCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "primaryTextCell"
}, asserts: [
  "ResourceTableCell renders the PrimaryTextCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const MutedTextCell: Story = {
  name: "MutedTextCell",
  parameters: { docs: { description: { story: "Tests the MutedTextCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "mutedTextCell"
}, asserts: [
  "ResourceTableCell renders the MutedTextCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const BadgeMappedCell: Story = {
  name: "BadgeMappedCell",
  parameters: { docs: { description: { story: "Tests the BadgeMappedCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "badgeMappedCell"
}, asserts: [
  "ResourceTableCell renders the BadgeMappedCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const BooleanCell: Story = {
  name: "BooleanCell",
  parameters: { docs: { description: { story: "Tests the BooleanCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "booleanCell"
}, asserts: [
  "ResourceTableCell renders the BooleanCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const DragHandleCell: Story = {
  name: "DragHandleCell",
  parameters: { docs: { description: { story: "Tests the DragHandleCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "dragHandleCell"
}, asserts: [
  "ResourceTableCell renders the DragHandleCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const ActionsCell: Story = {
  name: "ActionsCell",
  parameters: { docs: { description: { story: "Tests the ActionsCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "actionsCell"
}, asserts: [
  "ResourceTableCell renders the ActionsCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const OverflowCell: Story = {
  name: "OverflowCell",
  parameters: { docs: { description: { story: "Tests the OverflowCell scenario for ResourceTableCell with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <ResourceTableCell {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "overflowCell"
}, asserts: [
  "ResourceTableCell renders the OverflowCell scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
