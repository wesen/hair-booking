# Tasks

## Completed

- [x] Replace template placeholders with a real `hair-booking` module, binary, and README.
- [x] Add a Glazed/Cobra `serve` command with auth configuration flags.
- [x] Implement Keycloak OIDC login, callback handling, logout, and signed browser sessions.
- [x] Add `/api/info`, `/api/me`, static asset serving, and SPA fallback behavior.
- [x] Add automated tests and verify build plus HTTP smoke-test behavior.
- [x] Write the design guide and investigation diary in the ticket workspace.
- [x] Upload the ticket bundle to reMarkable and verify the remote listing.
