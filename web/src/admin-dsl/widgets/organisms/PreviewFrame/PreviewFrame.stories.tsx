/**
 * GENERATED SCAFFOLD — REVIEW BEFORE PROMOTING TO FINAL IMPLEMENTATION.
 *
 * Generated by: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/scripts/05-scaffold-admin-dsl-widgets.py
 * Generated at: 2026-05-18T21:21:08+00:00
 * Source YAML: ttmp/2026/05/15/HAIR-041--real-admin-backend-for-intake-app/sources/admin-dsl-widget-ir/08-media-widgets.yaml
 * Source YAML last commit: e555038 2026-05-18T17:11:02-04:00 HAIR-041 Step 48: Fill widget IR YAML intent
 * Target file previous commit: none (untracked or no history)
 * Widget ID: admin.media.preview-frame
 *
 * This file is generated from schema-v2 Widget Definition IR. Keep raw Admin DSL
 * JSON decoding in adapters; generated widgets should receive typed props only.
 */
import type { Meta, StoryObj } from "@storybook/react";
import { PreviewFrame } from "./PreviewFrame";
import type { PreviewFrameProps } from "./PreviewFrame.types";

const defaultArgs = {
  previewId: "customer-preview",
  title: "Customer intake preview",
  url: "/dsl-goja-demo/service",
  height: 420,
} as unknown as PreviewFrameProps;

const meta = {
  title: "Admin DSL Widgets/Organisms/PreviewFrame",
  component: PreviewFrame,
  args: defaultArgs,
  parameters: {
    docs: {
      description: {
        component: "PreviewFrame widget.",
      },
    },
  },
} satisfies Meta<typeof PreviewFrame>;

export default meta;
type Story = StoryObj<typeof meta>;

export const IframeConnected: Story = {
  name: "IframeConnected",
  parameters: { docs: { description: { story: "Tests the IframeConnected scenario for PreviewFrame with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <PreviewFrame {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "iframeConnected"
}, asserts: [
  "PreviewFrame renders the IframeConnected scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const Placeholder: Story = {
  name: "Placeholder",
  parameters: { docs: { description: { story: "Tests the Placeholder scenario for PreviewFrame with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <PreviewFrame {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "placeholder"
}, asserts: [
  "PreviewFrame renders the Placeholder scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const WithBodyAndActions: Story = {
  name: "WithBodyAndActions",
  parameters: { docs: { description: { story: "Tests the WithBodyAndActions scenario for PreviewFrame with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <PreviewFrame {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "withBodyAndActions"
}, asserts: [
  "PreviewFrame renders the WithBodyAndActions scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const TallPreview: Story = {
  name: "TallPreview",
  parameters: { docs: { description: { story: "Tests the TallPreview scenario for PreviewFrame with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <PreviewFrame {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "tallPreview"
}, asserts: [
  "PreviewFrame renders the TallPreview scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
export const MobilePreview: Story = {
  name: "MobilePreview",
  parameters: { docs: { description: { story: "Tests the MobilePreview scenario for PreviewFrame with representative fixtures and assertions." } } },
  args: {
    ...defaultArgs,
  },
  render: (args) => (
    <div style={{ padding: 24, maxWidth: 1120 }}>
      <PreviewFrame {...args} />
      <details style={{ marginTop: 12, fontSize: 12, color: "#6f6254" }}>
        <summary>Story fixture and assertions</summary>
        <pre style={{ whiteSpace: "pre-wrap" }}>{JSON.stringify({ fixtures: {
  "scenario": "mobilePreview"
}, asserts: [
  "PreviewFrame renders the MobilePreview scenario without layout breakage.",
  "The relevant semantic state is visible to users and test selectors."
] }, null, 2)}</pre>
      </details>
    </div>
  ),
};
